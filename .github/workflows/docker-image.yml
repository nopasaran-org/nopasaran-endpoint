name: Docker

on:
  push:
    branches:
      - main

jobs:
  build-worker-amd:
    runs-on: Linux_x64
    steps:
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1

      - name: Checkout code
        uses: actions/checkout@v2

      - name: Log in to Docker Hub
        run: echo ${{ secrets.DOCKER_TOKEN }} | docker login -u ${{ secrets.DOCKER_USERNAME }} --password-stdin

      - name: Build and push Docker image for benilies/nopasaran-worker
        run: |
          docker buildx build . \
            --file worker.Dockerfile \
            --tag benilies/nopasaran-worker:latest \
            --platform linux/amd64 \
            --push

  build-worker-arm64:
    runs-on: Linux_ARM64
    steps:
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1

      - name: Checkout code
        uses: actions/checkout@v2

      - name: Log in to Docker Hub
        run: echo ${{ secrets.DOCKER_TOKEN }} | docker login -u ${{ secrets.DOCKER_USERNAME }} --password-stdin

      - name: Build and push Docker image for benilies/nopasaran-worker
        run: |
          docker buildx build . \
            --file worker.Dockerfile \
            --tag benilies/nopasaran-worker:latest \
            --platform linux/arm64 \
            --push

  build-worker-arm64-v8:
    runs-on: Linux_ARM64
    steps:
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1

      - name: Checkout code
        uses: actions/checkout@v2

      - name: Log in to Docker Hub
        run: echo ${{ secrets.DOCKER_TOKEN }} | docker login -u ${{ secrets.DOCKER_USERNAME }} --password-stdin

      - name: Build and push Docker image for benilies/nopasaran-worker
        run: |
          docker buildx build . \
            --file worker.Dockerfile \
            --tag benilies/nopasaran-worker:latest \
            --platform linux/arm64/v8 \
            --push

  build-master-amd:
    runs-on: Linux_x64
    steps:
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1

      - name: Checkout code
        uses: actions/checkout@v2

      - name: Log in to Docker Hub
        run: echo ${{ secrets.DOCKER_TOKEN }} | docker login -u ${{ secrets.DOCKER_USERNAME }} --password-stdin

      - name: Build and push Docker image for benilies/nopasaran-master
        run: |
          docker buildx build . \
            --file master.Dockerfile \
            --tag benilies/nopasaran-master:latest \
            --platform linux/amd64 \
            --push

  build-master-arm64:
    runs-on: Linux_ARM64
    steps:
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1

      - name: Checkout code
        uses: actions/checkout@v2

      - name: Log in to Docker Hub
        run: echo ${{ secrets.DOCKER_TOKEN }} | docker login -u ${{ secrets.DOCKER_USERNAME }} --password-stdin

      - name: Build and push Docker image for benilies/nopasaran-master
        run: |
          docker buildx build . \
            --file master.Dockerfile \
            --tag benilies/nopasaran-master:latest \
            --platform linux/arm64 \
            --push

  build-master-arm64-v8:
    runs-on: Linux_ARM64
    steps:
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1

      - name: Checkout code
        uses: actions/checkout@v2

      - name: Log in to Docker Hub
        run: echo ${{ secrets.DOCKER_TOKEN }} | docker login -u ${{ secrets.DOCKER_USERNAME }} --password-stdin

      - name: Build and push Docker image for benilies/nopasaran-master
        run: |
          docker buildx build . \
            --file master.Dockerfile \
            --tag benilies/nopasaran-master:latest \
            --platform linux/arm64/v8 \
            --push
